# ======================================
# CONFIGURACIÓN DE BASE DE DATOS
# ======================================

# Base de datos de DESARROLLO (para pruebas locales)
DATABASE_URL_DEV=postgresql://usuario:password@host:5432/database_dev

# Base de datos de PRODUCCIÓN (datos reales - solo en Replit Secrets)
DATABASE_URL=postgresql://usuario:password@host:5432/database_prod

# ⚠️ IMPORTANTE: DATABASE_URL_DEV y DATABASE_URL deben ser DIFERENTES
# Nunca uses la misma base de datos para desarrollo y producción

# ======================================
# CONFIGURACIÓN DE SESIONES
# ======================================

# Secreto para firmar sesiones (genera uno aleatorio único)
# Puedes generar uno con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=cambia-esto-por-un-secreto-aleatorio

# ======================================
# CONFIGURACIÓN DE SERVICIOS EXTERNOS
# ======================================

# API de AeroDataBox (para información de aeropuertos y vuelos)
AERODATABOX_API_KEY=tu-api-key-aqui

# ======================================
# INSTRUCCIONES
# ======================================
# 
# 1. Copia este archivo a .env:
#    cp .env.example .env
#
# 2. Reemplaza los valores con tus credenciales reales
#
# 3. NUNCA subas .env a Git (ya está en .gitignore)
#
# 4. En Replit, usa la pestaña "Secrets" en lugar de .env
#
